
  # Create Layout

  This is a code bundle for Create Layout. The original project is available at https://www.figma.com/design/sZykDpiLLtdjZyt2QjXMOU/Create-Layout.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  